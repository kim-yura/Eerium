extends layout.pug

include utils.pug

block content
  +validationErrorSummary(errors)
  form(action=`/stories/${story.id}/edit` method='post')
    include story-form-fields.pug
    div(class='')
      button(type='submit' class='') Update Story
      a(href='/' class='') Cancel